[project]
name = "launch-ops"
version = "0.1.0"
description = "A Python project for exploring API integration, backend engineering, performance, and MCP development."
readme = "README.md"
requires-python = ">=3.13"
dependencies = []